{
  "name": "inkwell",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.js",
    "deploy": "node build.js --deploy",
    "dry-run": "node build.js --dry-run"
  },
  "dependencies": {
    "stripe": "^14.25.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
